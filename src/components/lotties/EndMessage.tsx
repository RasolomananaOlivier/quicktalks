import { Typography } from "@mui/material";
import React from "react";

const EndMessage = () => {
  return (
    <div>
      <Typography variant="h6">Look like there's no more message</Typography>
    </div>
  );
};

export default EndMessage;
